import {describe, it} from 'node:test'
import assert from 'node:assert'

import { sample, full } from './input.mjs'
import {
  solvePart1,
  solvePart2,
} from './solution.mjs'

console.log('\n\n\n\n')

describe('solvePart1', () => {
  describe('placeholder', (t) => {
    assert.deepEqual(
      'foo',
      'bar'
    )
  })
})
