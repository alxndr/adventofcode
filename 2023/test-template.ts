import {describe, it} from 'node:test'
import assert from 'node:assert'

// import {sample, full} from './input.ts'
// import {} from './solution.ts'

const IS_DEBUGGING = Boolean(process.env.IS_DEBUGGING) || false

console.log('\n2023 day ## testing...');

describe('part 1', () => {
  it('is a placeholder', (_t) => {
    assert.deepEqual(
      'foo',
      'bar'
    )
  })
})

describe.skip('part 2', () => {
  it('is a placeholder', (_t) => {
    assert.deepEqual(
      'foo',
      'bar'
    )
  })
})

